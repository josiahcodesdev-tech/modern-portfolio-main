# Copy to .env.local and fill in values from your Supabase Connect dialog.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# A legacy anon key can instead be provided as NEXT_PUBLIC_SUPABASE_ANON_KEY.
# Never use a secret/service-role key in NEXT_PUBLIC_ variables.
